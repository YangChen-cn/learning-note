# /proc 文件系统

## 是什么

`/proc` 是 Linux 内核暴露给用户态的**虚拟文件系统**：不占磁盘，内容由内核实时生成。读它不需要驱动、不需要特殊权限（大部分），是嵌入式 Linux 用户态开发最常用的数据来源。

（另：`/sys` 是设备模型（sysfs），后面学 GPIO、背光时会用到，先记着区别。）

## 常用文件

| 文件 | 内容 | 用途 |
|---|---|---|
| `/proc/uptime` | 开机总秒数 + 空闲总秒数 | 运行时间 |
| `/proc/meminfo` | 内存各项信息（单位 kB） | 内存监控 |
| `/proc/loadavg` | 1/5/15 分钟平均负载 | 负载监控 |
| `/proc/cpuinfo` | CPU 型号、特性 | 硬件识别 |
| `/proc/stat` | 内核统计（CPU 时间等） | CPU 占用率 |

## 查看方法

```sh
cat /proc/uptime
cat /proc/meminfo
```

## 两个练习读过的内容

- `uptime`：一行两个数字——`开机总秒数 空闲总秒数`（逗号前的数字）
- `meminfo`：多行，每行格式 `名称: 数值 单位`，例如：

  ```
  MemTotal:       1024000 kB
  MemFree:        524288 kB
  ```

  单位是 kB（1024 字节），除以 1024 就是 MB。

## 关键认知

`/proc` 下的文件本质是**文本行**，所以 C 程序做的事情就三件：**打开 → 读 → 解析**。三个练习都在重复这个套路，熟了就入门了。

（开发板上的 `BOARD_NOTES.md` 里那些 CPU/内存/网络信息，全部来自这些 /proc 文件。）
