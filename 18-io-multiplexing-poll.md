# I/O 多路复用：poll 机制与事件主循环（第 14 课预习）

## 本课目标

在练习 13 中，我们成功实现了独立的 Unix Domain Socket 服务端和客户端。

但是，当前的服务端存在一个**极其致命的架构缺陷——“单点阻塞死等”**：

```c
while (g_running) {
    client_fd = accept(server_fd, ...); // 阻塞 1：死等客户端敲门
    read(client_fd, ...);               // 阻塞 2：死等客户端发送请求
    write_all(client_fd, ...);
    close(client_fd);
}
```

### 痛点场景：
1. **慢速/恶意客户端拖垮服务**：如果客户端 A `connect()` 连接成功了，但它卡住了或者迟迟不发送 `"meminfo\n"`，服务端就会一直卡在 `read(client_fd)` 上！此时后面的客户端 B、C 哪怕想连接，也会被晾在门外排队，整个服务**彻底失去响应**。
2. **无法同时兼顾多个输入源**：在嵌入式边缘终端（`imx6u-edgepanel`）中，程序不仅要处理 Socket 请求，还要同时监听：
   - 终端键盘输入（标准输入 `STDIN_FILENO`）
   - 触摸屏点击事件（`/dev/input/event1`）
   - 传感器数据变化
   - 定时上报事件

如果每一个都用阻塞的方式读，单线程程序根本分身乏术。

本课的目标就是引入 **I/O 多路复用技术（I/O Multiplexing）——`poll()`**，让单进程可以**同时监听多个文件描述符**，谁有数据就处理谁，彻底告别单点阻塞！

---

## 一、I/O 多路复用是什么？

### 生活中的比喻：
- **普通阻塞 I/O（傻等）**：你去多个快递驿站取快递。你在 A 驿站门口死等，直到取到；再去 B 驿站门口死等，哪怕 C 驿站的快递其实早就到了。
- **I/O 多路复用（智能前台）**：你雇了一个管家（`poll`），把 A、B、C 驿站的提单号全交给他。管家一有包裹到达就通知你：“A 驿站有件了，去拿！”你只在有数据的时候去处理，绝对不白白浪费时间等待。

在 Linux 中，有三大多路复用系统调用：
- `select`：最早出现，但有 1024 个 fd 的数量上限，且每次内核都要全盘线性扫描，逐渐淘汰。
- **`poll`**：**克服了 1024 限制，结构体清晰**，代码直观，非常适合中等并发（数十个设备/连接）的嵌入式边缘网关和事件主循环。
- `epoll`：Linux 专属高并发霸主（Nginx/Redis 底层），适合数万连接。但在嵌入式小并发场景下略显繁琐。

---

## 二、`poll()` 系统调用详解

头文件：
```c
#include <poll.h>

int poll(struct pollfd *fds, nfds_t nfds, int timeout);
```

### 1. 核心结构体：`struct pollfd`
```c
struct pollfd {
    int   fd;         // 要监听的文件描述符（如 server_fd、client_fd、STDIN_FILENO）
    short events;     // 【输入】我们关心什么事件（例如 POLLIN）
    short revents;    // 【输出】内核通知实际发生了什么事件（returned events）
};
```

#### 常用的事件标志位：
| 标志位 | 含义 | 什么时候触发 |
| :--- | :--- | :--- |
| **`POLLIN`** | **有数据可读** | 套接字收到数据、新客户端连接到达（可调 `accept`）、终端有键盘输入 |
| **`POLLOUT`** | **可以写入** | 写缓冲区有空闲空间，`write` 不会阻塞 |
| **`POLLHUP`** | **对端挂断连接** | 客户端主动关闭了连接（内核自动填入 `revents`，无需事先在 `events` 订阅） |
| **`POLLERR`** | **发生错误** | 描述符出错（内核自动填入 `revents`） |

---

### 2. `poll()` 函数参数与返回值

- **`fds`**：`struct pollfd` 类型的数组首地址。
- **`nfds`**：数组中的元素个数。
- **`timeout`**：超时时间（单位：**毫秒 ms**）：
  - **`-1`**：**永久阻塞**，直到至少有一个被监听的 fd 发生事件才返回；
  - **`0`**：**立即非阻塞返回**（轮询测一下就走）；
  - **`> 0`**：最多等待指定的毫秒数，若超时仍无事件则返回 `0`。

#### 返回值：
- **`> 0`**：成功，返回**发生事件的文件描述符个数**。
- **`0`**：超时，没有任何描述符发生事件。
- **`-1`**：出错（如被信号中断 `errno == EINTR`）。

---

## 三、标准使用范式与代码骨架

```c
struct pollfd fds[2];

// 监听 1：标准输入（键盘敲击）
fds[0].fd = STDIN_FILENO;
fds[0].events = POLLIN;

// 监听 2：服务端监听套接字（新客户端敲门）
fds[1].fd = server_fd;
fds[1].events = POLLIN;

while (g_running) {
    // 阻塞等待，直到标准输入有敲击，或者有客户端连接
    int ret = poll(fds, 2, -1);

    if (ret < 0) {
        if (errno == EINTR) continue; // 被信号打断
        perror("poll");
        break;
    }

    // 检查标准输入是否有数据
    if (fds[0].revents & POLLIN) {
        char buf[64];
        read(STDIN_FILENO, buf, sizeof(buf));
        printf("检测到键盘输入: %s", buf);
    }

    // 检查服务端套接字是否有新连接
    if (fds[1].revents & POLLIN) {
        int client_fd = accept(server_fd, NULL, NULL);
        printf("新客户端连接进来了！client_fd = %d\n", client_fd);
        // 处理客户端...
    }
}
```

---

## 四、为什么说 `poll` 是嵌入式系统主循环（Event Loop）的核心？

在正点原子 I.MX6U 开发板上，边缘终端的主程序通常就是这样一个事件循环：

```text
               ┌───────────────────────────────┐
               │    poll() 统一多路等待入口      │
               └───────────────┬───────────────┘
                               │ 唤醒并分发事件
        ┌──────────────────────┼──────────────────────┐
        ▼                      ▼                      ▼
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│  触摸屏事件到达   │   │  本地 Socket 请求 │   │   定时器超时到达   │
│(/dev/input/event1)│   │ (/tmp/monitor.sock)│   │  (timerfd/心跳)  │
│  更新 UI 界面坐标 │   │   返回 CPU/内存   │   │   刷新状态指示灯 │
└──────────────────┘   └──────────────────┘   └──────────────────┘
```

使用 `poll`，你只需要一个单线程程序，就能像多任务系统一样井井有条地响应各种外设输入！

---

## 五、本课练习规划（练习 14）

在接下来的练习中，我们将为服务端引入 `poll`：
1. 服务端使用 `poll()` 同时监听 **服务端监听套接字 `server_fd`** 和 **标准输入 `STDIN_FILENO`**；
2. 客户端连接来时，`server_fd` 触发 `POLLIN`，服务端调用 `accept` 处理；
3. 用户在运行服务端的终端中按下回车或输入命令（如输入 `quit\n` 或 `status\n`），`STDIN_FILENO` 触发 `POLLIN`，服务端立刻响应。
4. 彻底体验单线程如何从容并发响应多个来源！
